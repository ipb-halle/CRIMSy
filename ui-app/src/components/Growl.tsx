import React from "react";

const Growl: React.FC = () => {
    return <div id="growl" className="growlMessages" style={{ position: "fixed", bottom: "1rem", right: "1rem" }} />;
};

export default Growl;
